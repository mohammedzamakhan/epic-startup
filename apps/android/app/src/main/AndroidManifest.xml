<?xml version="1.0" encoding="utf-8"?>
<!--
  The app has exactly one Activity, no services, and no exported content
  providers. Everything the customer can do lives in MainActivity, which talks
  straight to the US control-plane App (branding) and the regional tenant-api
  (phone OTP + profile) — see docs/tenant-data-residency.md.
-->
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools">

	<uses-permission android:name="android.permission.INTERNET" />

	<!--
	  allowBackup=false (plus the API 31+ extraction rules) keeps the Keystore
	  session from following the customer to another device, and
	  enableOnBackInvokedCallback opts into predictive back on API 33+.
	-->
	<application
		android:allowBackup="false"
		android:dataExtractionRules="@xml/data_extraction_rules"
		android:enableOnBackInvokedCallback="true"
		android:fullBackupContent="false"
		tools:ignore="UnusedAttribute"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:networkSecurityConfig="@xml/network_security_config"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">

		<activity
			android:name=".ui.MainActivity"
			android:configChanges="keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout|uiMode"
			android:exported="true"
			android:windowSoftInputMode="adjustResize">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
	</application>

</manifest>
