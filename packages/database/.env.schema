# Control-plane database package
# See https://varlock.dev/guides/monorepos/
#
# @defaultRequired=infer @defaultSensitive=false
# @generateTsTypes(path=./src/env.ts, exposeEnv=local)
# ----------

# @required @type=url @sensitive=false
DATABASE_URL="file:./db/data.db?connection_limit=1"

# Fallback SQLite path when DATABASE_URL is cwd-relative and missing on disk
# @sensitive=false
DATABASE_PATH=
