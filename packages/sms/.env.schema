# SMS delivery package (Twilio, US node only)
# See https://varlock.dev/guides/monorepos/
#
# @currentEnv=$NODE_ENV
# @defaultRequired=infer @defaultSensitive=false
# @generateTsTypes(path=./src/env.ts, exposeEnv=local)
# ----------

# @type=enum(production, development, test)
NODE_ENV=development

# @type=enum(us, ksa) @sensitive=false
DATA_REGION=us

# @sensitive=false
TWILIO_ACCOUNT_SID=

# @sensitive
TWILIO_AUTH_TOKEN=

# @sensitive=false
TWILIO_FROM_NUMBER=
