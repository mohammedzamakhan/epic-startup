# Regional tenant SQLite package
# See https://varlock.dev/guides/monorepos/
#
# @defaultRequired=infer @defaultSensitive=false
# @generateTsTypes(path=./src/env.ts, exposeEnv=local)
# ----------

# Per-org SQLite directory on OCI block volume
# @sensitive=false
TENANT_DB_DIR=

# Fallback when resolving tenant DB path locally
# @sensitive=false
DATABASE_PATH=
